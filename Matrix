
<?xdofx:
sum(
  decode(LINE_DESCRIPTION,'BREAK FAST',(QUANTITY*100) div 80)
+ decode(LINE_DESCRIPTION,'LUNCH',(QUANTITY*100) div 80)
+ decode(LINE_DESCRIPTION,'DINNER',(QUANTITY*100) div 80)
+ decode(LINE_DESCRIPTION,'LODGING',(QUANTITY*100) div 80)
)
?>
