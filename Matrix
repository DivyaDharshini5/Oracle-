SELECT DISTINCT
    ppnfv.full_name AS APPROVER
FROM
    PER_PERSON_NAMES FV PPNFV
JOIN
    PO_PENDING popen
    ON ppnfv.PERSON_ID = popen.ASSIGNEES
WHERE
    ppnfv.full_name IS NOT NULL
ORDER BY
    ppnfv.full_name
