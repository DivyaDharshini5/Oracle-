-- 1️⃣ Check if you can see the projects view
SELECT owner, object_name, object_type
FROM all_objects
WHERE object_name = 'PJF_PROJECTS_ALL_VL';

-- 2️⃣ Check if the region mapping table exists
SELECT owner, object_name, object_type
FROM all_objects
WHERE object_name = 'XXDPW_POML_BU_REGION_MAPPING';

-- 3️⃣ Check if the budgets view exists
SELECT owner, object_name, object_type
FROM all_objects
WHERE object_name = 'PJF_PROJECT_BUDGETS_V';

-- 4️⃣ Check if the cost summary view exists
SELECT owner, object_name, object_type
FROM all_objects
WHERE object_name = 'XXDPW_POML_PROJ_COST_SUMMARY_V';
