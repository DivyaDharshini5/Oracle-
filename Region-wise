SELECT
    proj.project_id,
    proj.segment1 AS PROJECT_NUMBER,
    proj.name AS PROJECT_NAME,
    proj.status_code AS STATUS,
    proj.start_date,
    proj.completion_date
FROM
    PJF_PROJECTS_ALL_VL proj
WHERE
    proj.status_code NOT IN ('CANCELLED', 'CLOSED')
ORDER BY
    proj.segment1;
