SELECT DISTINCT
    COALESCE(
        poapp.L5_APPROVED_BY,
        poapp.L4_APPROVED_BY,
        poapp.L3_APPROVED_BY,
        poapp.L2_APPROVED_BY,
        poapp.L1_APPROVED_BY
    ) AS SELECTED_APPROVER
FROM PO_APPROVAL poapp
WHERE COALESCE(
        poapp.L5_APPROVED_BY,
        poapp.L4_APPROVED_BY,
        poapp.L3_APPROVED_BY,
        poapp.L2_APPROVED_BY,
        poapp.L1_APPROVED_BY
      ) IS NOT NULL
ORDER BY SELECTED_APPROVER
